# 사안별 뉴스 업데이트 — 검토용 구현

같은 사안의 후속 기사는 기존 Telegram 메시지를 편집한다. 새 메시지를 반복해서 보내지 않고 기사 수, 최근 보도 제목 최대 3개, 최근 원문 최대 5개를 갱신한다. 아침·저녁 브리핑은 갱신된 사안 최대 5개와 메시지 이동 링크를 모은다. 남은 사안은 다음 브리핑으로 이월한다.

## 동작

- `keywords.json`의 `story_mode: true`로 활성화하며 false이면 기존 동작으로 복귀한다.
- 동일 URL은 다시 처리하지 않는다. 기존 `sent` 기록을 존중하므로 전환 시 과거 기사 전체를 재발송하지 않는다. 기존 pending은 새 모드로 옮긴다.
- 제목의 공통 단어 2개 이상, Jaccard 유사도 0.45 이상, 최초 기사와 72시간 이내를 모두 만족해야 묶는다. 회사명이 같아도 노사·안전·투자·채용 쟁점이 다르면 분리한다. 대표 제목을 기준으로 비교해 연쇄 오묶음을 줄인다.
- 일반 뉴스는 브리핑을 기다린다. 기존 instant 키워드는 우선 알림, instant_title은 야간 대기와 회당 상한을 적용한다. 이들 기사도 같은 사안이면 기존 메시지를 갱신한다.
- 주요 후속 보도 알림은 우선 알림 대상 사안에서 합의·사망·파업 돌입 표지가 새로 등장했을 때만 보낸다. 가능성·촉구·요구·결렬 등의 표현은 보수적으로 제외한다. 동일 표지는 사안별 1회만 알린다.
- 일반 갱신은 기존 메시지 편집만 수행한다. 신규 일반 메시지와 브리핑은 무음이다.
- 기사 묶음은 본문 요약이 아니다. 화면에 ‘최근 보도 제목’으로 명시하며, 사실을 새로 생성하지 않는다.

## 발송·복구

`sendMessage` 응답의 message_id를 보존하고 `editMessageText`로 수정한다. 성공한 사안마다 state.json을 임시 파일과 원자적 교체로 저장한다. Telegram 응답이 not modified이면 성공으로 간주하며, message to edit not found일 때만 새 메시지를 만든다. 권한 오류·전송 오류는 중단한다.

getMe/getChat으로 실제 봇과 채널을 실행 로그에 표시하고, 저장된 봇 ID 또는 채널 ID가 바뀌면 기존 메시지를 수정하기 전에 중단한다. 토큰은 출력·저장하지 않는다. 최초 실행 전 테스트 채널의 봇 계정과 실제 실행 경로를 확인해야 한다.

기존 workflow의 always 상태 저장을 활용하므로 뒤쪽 게시판 단계가 실패해도 디스크에 저장된 뉴스 상태를 커밋할 수 있다.

## 검증

`python -m unittest -v test_delivery.py test_stories.py`

실제 Telegram으로 보내지 않고 가짜 응답으로 묶기, 후속 편집, 재실행 중복, 주요 변화 알림, 오류 보존, 삭제된 메시지, 채널 변경, 야간 대기, 브리핑 이월 등을 검증한다.

## 남은 한계

- 의미가 같지만 제목 표현이 크게 다르면 별도 사안으로 남을 수 있다. 같은 제목의 별도 사건을 잘못 묶을 가능성도 있어 실제 기사 표본 검토가 필요하다.
- 본문 기반 요약·사실 변화 추출은 미구현이다. 주요 변화 감지도 제목 규칙이므로 완전하지 않다.
- 기존 화면에 있던 봇과 저장소의 출력 형식이 달라 실제 실행 경로 확인 전 운영 반영하지 않았다.
- 기존 게시판 공고 모듈은 이번 사안 묶기 대상이 아니다.
- 최근 원문 5개만 메시지에 표시한다. 전체 수집 기사는 상태에 보관한다. 아직 보관 기간 자동 정리는 없다.
- Telegram 수신 성공과 상태 저장 사이의 강제 종료, 저장소 push 실패는 중복을 남길 수 있다. 완전한 exactly-once 발송을 보장하지 않는다.
- 상태에 message_id가 없는 과거 메시지는 소급해서 편집하지 않는다.

API 참고: https://core.telegram.org/bots/api#editmessagetext
