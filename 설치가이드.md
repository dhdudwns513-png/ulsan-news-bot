# 울산 동구 뉴스봇 설치 가이드 (약 15분)

## 1. 텔레그램 봇 만들기 (2분)
1. 텔레그램에서 **@BotFather** 검색 → `/newbot`
2. 봇 이름(예: 동구뉴스봇), 아이디(예: `donggu_news_bot`) 입력
3. 발급되는 **토큰** 복사 → `TELEGRAM_BOT_TOKEN`

## 2. 채팅 ID 알아내기 (2분)
- **개인 수신**: 만든 봇에게 아무 메시지나 보낸 뒤, 브라우저에서
  `https://api.telegram.org/bot<토큰>/getUpdates` 열기 → `"chat":{"id":123456789` 숫자가 ID
- **단체방 수신**: 봇을 단체방에 초대하고 방에서 아무 말이나 한 뒤 위 주소 열기 → ID는 `-100...`으로 시작하는 음수

## 3. GitHub 저장소 만들기 (5분)
1. https://github.com/new → 이름 `ulsan-news-bot`, **Private** 선택, Create
2. **Add file → Upload files** 로 이 폴더의 파일 전부 업로드
   - `news_bot.py`, `keywords.json`, `state.json`, `requirements.txt`, `.github/workflows/news.yml`
   - ⚠ `.github/workflows/` 폴더 구조가 유지돼야 함. 업로드 창에 폴더째 드래그하면 됨
3. **Settings → Secrets and variables → Actions → New repository secret** 으로 2개 등록
   | 이름 | 값 |
   |---|---|
   | `TELEGRAM_BOT_TOKEN` | 1단계 토큰 |
   | `TELEGRAM_CHAT_ID` | 2단계 ID |
4. **Actions** 탭 → "울산 동구 뉴스봇" → **Run workflow** → 텔레그램에 브리핑이 오면 성공

## 운영
- **키워드 수정**: GitHub에서 `keywords.json` 열고 연필 아이콘 → 수정 → Commit. 휴대폰 브라우저로도 됨
- **발송 시간 변경**: `.github/workflows/news.yml`의 cron (UTC 기준, 한국시간 −9시간)
- **긴급 알림**: `urgent_keywords`에 든 검색어는 09~22시 매시간 확인해 새 기사 즉시 발송
- 뉴스 수집원: 구글 뉴스 검색 RSS (키 불필요). 네이버 API HUB 키를 나중에 등록하면 자동으로 함께 사용
- 비용: 전부 무료 (GitHub Actions 무료 한도 월 2,000분 중 약 300분 사용)
